<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">
	<context:property-placeholder location="classpath:db.properties"/>
    <bean id="hikariPool" class="com.zaxxer.hikari.HikariDataSource"> 
		<property name="dbUrl" value="${db.url}"/>
		<property name="dbUser" value="postgres"/>
		<property name="dbPassword" value="qwerty007"/>
		<property name="dbDriverName" value="org.postgresql.Driver"/>
	</bean>
	<bean id="hikariPool" class="com.zaxxer.hikari.HikariDataSource"> 
		<property name="dbUrl" value="${db.url}"/>
		<property name="dbUser" value="${db.user}"/>
		<property name="dbPassword" value="${db.password}"/>
		<property name="dbDriverName" value="${db.driver.name}"/>
	</bean>

</beans>
